import React from 'react';
import DefaultLayout from '../basics/DefaultLayout';

const LoadingScreen: React.FC = () => {
  return (
    <DefaultLayout className="LoadingScreen">
      …
    </DefaultLayout>
  );
};

export default LoadingScreen;
